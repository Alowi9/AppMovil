 ///// 3) Escribir el código de una función a la que se pasa como parámetro un número entero y devuelve 
 como resultado una cadena de texto que indica si el número es par o impar. 
 Mostrar por pantalla el resultado devuelto por la función.////

function esPar (num){
    if( num % 2 == 0 ){
        return  console.log("par");
    }
    return console.log("impar");

}

esPar(4);
esPar(1);
esPar(5);
esPar(6);
